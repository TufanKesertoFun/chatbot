{
  "name": "emlak-chat-server",
  "version": "1.0.0",
  "description": "Emlak Chat PoC Backend",
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/app.js",
    "start": "node src/app.js",
    "migrate": "npx prisma migrate dev",
    "seed": "node prisma/seed.js",
    "db:reset": "npx prisma migrate reset --force --skip-generate && npx prisma generate && node prisma/seed.js",
    "db:rebuild": "npm run db:reset"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/jwt": "^8.0.0",
    "@fastify/static": "^7.0.1",
    "@google-cloud/translate": "^8.1.0",
    "@google/genai": "^1.40.0",
    "@prisma/client": "^5.10.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "fastify": "^4.26.1",
    "fastify-socket.io": "^5.1.0",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "prisma": "^5.10.0"
  }
}
